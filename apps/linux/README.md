# Linux application

Reserved for a future native Linux KeyClick implementation. It must consume the contracts under `shared/specs` and preserve stable input identifiers where the platform exposes equivalent information.
