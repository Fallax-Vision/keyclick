# macOS application

Reserved for a future native macOS KeyClick implementation. It must consume the contracts under `shared/specs` and preserve stable input identifiers where the platform exposes equivalent information.
